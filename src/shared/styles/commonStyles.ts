import { makeStyles } from "../../style";

export const useStyles = makeStyles()(() => ({
    headerLogo: {
        display: 'flex',
        flexDirection: 'row',
        background: 'black',
        height: '100px',
    },
}));