import { makeStyles } from "../../style";

export const useStyles = makeStyles()(() => ({
    header: {
        display: 'flex',
        flexDirection: 'row',
        background: 'black',
        height: '100px',
    },
    header__wrapper: {

    }
}));
